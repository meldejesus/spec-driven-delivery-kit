# Tag Registry

> The canonical tag taxonomy is at `workflow/TAGS.md`.
>
> That file covers the full three-layer system: `area/`, `type/`, and `component/` prefixes, YAML frontmatter usage, Obsidian Dataview support, and bash search commands.

Do not maintain tags here — use `workflow/TAGS.md` as the single source of truth.
